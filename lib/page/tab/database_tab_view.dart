import 'package:flutter/material.dart';

class DatabaseTabView extends StatelessWidget {
  const DatabaseTabView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
